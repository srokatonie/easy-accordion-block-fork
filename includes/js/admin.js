(function (window, wp, $) {
	// // custom link's id
	// const linkId = 'esab-template-library';
	// // button's html
	// const linkHtml =
	// 	'<a id="' +
	// 	linkId +
	// 	'" class="components-button" href="#" >Template Library</a>';
	// // check if gutenberg's editor root element is present.
	// const editorEl = document.getElementById('editor');
	// if (!editorEl) {
	// 	// do nothing if there's no gutenberg root element on page.
	// 	return;
	// }
	// wp.data.subscribe(function () {
	// 	setTimeout(function () {
	// 		if (!document.getElementById(linkId)) {
	// 			const toolbalEl = editorEl.querySelector(
	// 				'.edit-post-header__toolbar'
	// 			);
	// 			if (toolbalEl instanceof HTMLElement) {
	// 				toolbalEl.insertAdjacentHTML('beforeend', linkHtml);
	// 			}
	// 		}
	// 	}, 1);
	// });
})(window, wp, jQuery);
